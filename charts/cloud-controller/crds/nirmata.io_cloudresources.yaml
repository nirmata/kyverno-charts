---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.0
  name: cloudresources.nirmata.io
spec:
  group: nirmata.io
  names:
    kind: CloudResource
    listKind: CloudResourceList
    plural: cloudresources
    singular: cloudresource
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CloudResource is a generic resource in cloud.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: Cloud resource configuration spec.
            properties:
              cloudProvider:
                description: CloudProvider is the cloud service provider.
                type: string
              region:
                description: Region is the region of the resource.
                type: string
              resourceArn:
                description: ResourceArn is the ARN of the resource.
                type: string
              resourceData:
                description: |-
                  ResourceData is the data fetched and populated in the resource.
                  Mark as schemaless and preserve unknown fields so arbitrary service payloads (ecs/lambda/etc.) are accepted.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              resourceName:
                description: ResourceName is the name of the resource.
                type: string
              resourceType:
                description: ResourceType is the type of the resource.
                type: string
              scanInterval:
                default: 1h
                description: |-
                  ScanInterval defines the interval in duration at which to scan the cloud resources.
                  The duration is a sequence of decimal numbers, each with optional fraction and a unit suffix,
                  such as "300ms", "1.5h" or "2h45m". Valid time units are "ns", "us" (or "µs"), "ms", "s", "m", "h".
                format: duration
                type: string
            required:
            - cloudProvider
            - region
            - resourceArn
            - resourceName
            - resourceType
            type: object
          status:
            description: Status contains the current state of the CloudResource
            properties:
              discoveryStatus:
                description: DiscoveryStatus is the status of the discovery of the
                  cloud resource.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              lastUpdated:
                description: LastUpdated is the last time this CloudResource was synced
                format: date-time
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
